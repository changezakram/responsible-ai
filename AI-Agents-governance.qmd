---
title: "Agentic AI Governance"
subtitle: "Why Agentic AI Changes the Governance Problem"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    theme: cosmo
---

## Introduction

For decades, financial institutions have governed AI largely through frameworks built around models: define the intended use, validate performance, establish controls, approve deployment, and monitor for deterioration. This approach works reasonably well when the object being governed is a bounded model operating within a defined process. Agentic AI changes that assumption: an agent can reason across multiple steps, select and use tools, maintain state, and take actions toward an objective. As autonomy increases, the governance question expands from **"Is this model performing as intended?"** to **"Is this system behaving as intended, and does it still have the appropriate authority to act?"**

The distinction is increasingly visible in financial regulation. U.S. banking regulators revised their model risk management guidance in 2026 and explicitly excluded generative and agentic AI from scope — a notable admission that the supervisory framework doesn't yet know how to answer that second question. The Financial Stability Board has taken a similar position internationally. Neither has published a rulebook; both have published direction.

Model validation, performance monitoring, explainability, fairness, and data controls remain necessary; they're just no longer sufficient on their own. Agentic systems introduce additional questions: What objective is the agent authorized to optimize, and toward what actions? Can its behavior change after approval? What happens when individually governed components interact? Could independently deployed agents produce correlated behavior or systemic risk?

These questions point toward a broader governance model — one that treats AI not as a model to validate once, but as a dynamic decision-making system to govern throughout execution. The next evolution of Responsible AI, particularly in financial services, will require a shift from model governance to system governance, and from governance expressed primarily through policies and review processes to controls that can be observed, enforced, and audited at runtime.


## Why Traditional Model Governance Is Not Enough

Traditional model risk management is built around a relatively stable unit of analysis: the model. A model is developed for an intended purpose, validated before use, approved within defined parameters, and monitored over time to determine whether its performance remains acceptable. Even sophisticated machine-learning models generally fit within this structure when their inputs, outputs, intended use, and decision boundaries can be clearly defined.

Agentic AI introduces a different operating model: an agent can interpret an objective, formulate a plan, retrieve information, select among available tools, interact with other systems or agents, and execute a sequence of actions. Its behavior is determined not only by the underlying model, but also by the context it receives, the tools and data it can access, its memory and state, the policies governing it, and the results of previous actions. The object being governed is a dynamic system of models, data, tools, permissions, workflows, and decisions — and that shift creates several distinct challenges for traditional governance.

### From Performance Drift to Behavioral Drift

Traditional model monitoring typically looks for deterioration in metrics such as accuracy, discrimination, calibration, stability, or forecast error. These remain important, but an agent can remain technically capable while changing how it pursues its objective.

Consider an AI agent supporting customer retention. Its objective might be to reduce attrition among high-value customers. Over time, the agent could discover that increasingly aggressive offers produce better short-term retention. Its predictive performance may remain strong — even improve — while its behavior gradually moves outside the institution's intended pricing, profitability, or customer-treatment boundaries.

The relevant governance question is therefore no longer only is the model still performing? It also becomes: is the agent still behaving in a manner consistent with what we approved? Recent research describes this problem as policy drift and proposes monitoring observed agent behavior for changes in decision policy, rather than relying exclusively on traditional performance metrics.

### From Outputs to Actions

Traditional AI systems often produce an output that a human or downstream application consumes: a probability of default, fraud score, customer segment, forecast, or recommendation. Agents can retrieve customer information, query multiple systems, determine the next step, call an API, initiate a workflow, communicate with another agent, or execute an authorized transaction. As the system moves from recommendation to execution, governance must address not only the quality of the output but also the authority attached to it.

A lending model might recommend that an application requires additional documentation. An agent could identify the missing information, contact the customer, retrieve documents, update the case, and advance the application through the workflow — each added capability expanding the governance surface. The question becomes not simply what can the AI determine? but what is the AI permitted to do as a consequence?

### From a Model Boundary to a System Boundary

Enterprise AI rarely operates in isolation. An agentic workflow might combine a foundation model, enterprise data, a retrieval system, memory, business rules, APIs, other agents, and a core application, each handing off to the next. Every component can satisfy its individual control requirements and the overall system can still produce an undesirable outcome.

For example, an LLM may correctly interpret a customer's request. A customer-data service may return accurate information. A pricing engine may correctly calculate an available offer. An API may execute exactly the instruction it receives. Yet the combination could still produce an offer that violates a business rule or gives the agent more discretion than intended — individually acceptable components do not necessarily produce an acceptable system. This is compositional risk: governance has to examine the end-to-end behavior of the workflow, not just the components that make it up.

### From Periodic Validation to Continuous Governance

Traditional governance is often organized around checkpoints: development review, independent validation, approval, periodic monitoring, and revalidation when material changes occur. That remains valuable, but autonomous systems introduce risks between those checkpoints.

An agent's operating environment can change continuously. Data changes. Permissions change. APIs change. Other agents take actions. Business conditions change. An action that was appropriate when a plan was generated may no longer be appropriate when it is executed. Runtime controls can include:

- verifying permissions before consequential actions,
- enforcing transaction or exposure limits,
- requiring human approval above defined thresholds,
- monitoring anomalous behavior,
- recording agent decisions and tool interactions,
- and stopping or escalating activity when policy boundaries are reached.

Governance increasingly lives inside the system's execution path, not just its review calendar.

### Traditional Model Governance Still Matters

None of this displaces the disciplines already in place. Conceptual soundness, independent validation, data quality, documentation, change management, and effective challenge remain essential — the practices above extend them, they don't replace them. In April 2026, the Federal Reserve, OCC, and FDIC reaffirmed these principles in revised model risk management guidance, while explicitly stating that generative and agentic AI fall outside its scope because they are novel and rapidly evolving, and directing banks to determine appropriate governance and controls through their broader risk-management frameworks instead.

The Financial Stability Board has taken a similarly broader view. Its 2026 proposed sound practices address organization-wide AI governance, the AI lifecycle, cybersecurity, technology, and third-party risk, and explicitly consider whether these practices adequately address emerging forms of AI such as generative and agentic systems.

Banks don't need to replace model governance; they need to extend the governance boundary. For traditional models, the model itself can often remain the primary unit of governance. For agentic AI, governance increasingly needs to encompass the entire decision system:

Objective → Model → Data → Memory → Tools → Authority → Actions → Outcomes

One question raised at the outset — whether independently deployed agents can produce correlated behavior or systemic risk — sits outside what any single institution can govern on its own, and is addressed separately in the sections that follow.


## From Model Governance to System Governance

If agentic AI expands the unit of risk beyond the model, governance must expand with it. The central shift is from governing an individual analytical artifact to governing an end-to-end decision system — one that may include foundation models, enterprise data, memory, retrieval mechanisms, business rules, APIs, tools, other agents, and the applications through which actions are ultimately executed. This changes both the scope and the timing of governance.

Traditional model governance asks whether a model is conceptually sound, appropriately validated, performing within acceptable limits, and being used for its intended purpose. System governance retains those questions but adds another: can the complete AI system pursue its objective, exercise its authority, and interact with its environment without moving outside the institution's risk appetite and control boundaries?

The shifts described in the previous section — from performance drift to behavioral drift, from outputs to actions, from a model boundary to a system boundary, and the systemic risk deferred from the introduction — map onto five interconnected governance dimensions.

### Objective Governance — What Is the Agent Optimizing?

Every autonomous system operates toward an objective, whether explicitly encoded or expressed through instructions, incentives, policies, and feedback, which makes the objective itself a governance concern. A collections agent optimized exclusively for recovery could pursue increasingly aggressive strategies. A fraud agent focused entirely on minimizing losses could create excessive customer friction. A marketing agent rewarded for conversion could favor offers that maximize immediate response over long-term customer value or suitability.

The relevant question isn't whether the agent achieves its objective — it's whether the objective, and the behaviors it incentivizes, stay aligned with business strategy, regulatory obligations, customer outcomes, and risk appetite.

### Authority Governance — What Is the Agent Allowed to Do?

This is the governance dimension behind the outputs-to-actions shift: intelligence and authority are different things, and an agent may be capable of determining an appropriate action without being authorized to execute it. An agent might be permitted to retrieve information but not modify it, prepare a transaction but not submit it, recommend an offer but not change pricing, or resolve routine cases while escalating exceptions to a human.

Authority governance sets these boundaries explicitly — what data an agent may access, which tools it may invoke, which actions it may execute, the financial or operational limits within which it may act, and when human approval is required — establishing bounded autonomy: enough authority to create value, without unrestricted control over consequential processes.

### Behavioral Governance — Is the Agent Still Acting as Intended?

This is the governance dimension behind the drift shift: approval isn't the end of governance, because an agent's behavior can evolve as its context, memory, data, tools, or environment change, and the system can stay technically functional while gradually pursuing its objective in ways validation never anticipated.

Behavioral governance focuses on conduct over time — decision patterns, tool usage, escalation behavior, exceptions, policy violations, unexpected action sequences, and deviations from approved boundaries — to determine whether decision behavior itself has changed materially, not just whether performance has deteriorated. For higher-risk agents, this requires continuous observability and the ability to intervene during operation.

### Composition Governance — Are the Interactions Safe?

This is the governance dimension behind the model-boundary-to-system-boundary shift: agentic systems are assembled from components — a foundation model from one provider, enterprise data on another platform, retrieval through a third service, agents interacting through APIs or delegating to each other — and every component can work correctly in isolation while their interaction produces an unexpected result.

Composition governance treats the workflow itself as the unit of risk, asking whether permissions are preserved when one agent delegates to another, whether untrusted retrieved content can influence a privileged action, whether information can cross system boundaries unintentionally, whether several individually permitted actions can combine into a prohibited outcome, and whether replacing one component changes the risk of the overall workflow. This matters most where AI capabilities are assembled from multiple internal and third-party services.

### Systemic Governance — What Happens When Many Agents Act Together?

This is the fourth question raised at the outset and deferred from the previous section. Financial institutions also need to consider risk beyond any individual workflow: AI systems increasingly consume similar datasets, use similar foundation models, respond to similar signals, and optimize against comparable objectives, so independently governed agents may arrive at similar decisions at the same time.

In financial markets, this can amplify crowded trades, liquidity pressure, rapid repricing, procyclical behavior, or operational concentration. Within a single institution, similar effects can occur when multiple agents interact with the same customers, limits, inventories, or operational processes. The governance question expands accordingly: what happens when individually acceptable AI decisions interact at scale? Traditional model-by-model validation is poorly positioned to observe this on its own.

## A Layered Governance Architecture

Taken together, these five dimensions form a chain, not five independent controls:

Objective → Authority → Behavior → Composition → Systemic

An inappropriate objective can drive undesirable behavior. Excessive authority turns that behavior into consequential action. Component interactions amplify those actions. Widespread deployment turns local failures into systemic ones. Governance has to operate across the full chain.

This also changes how Responsible AI gets implemented. Principles like fairness, accountability, transparency, safety, and human oversight remain essential, but they need to be translated into technical and operational controls: objectives become measurable constraints, authority becomes permissions and limits, behavioral expectations become runtime monitoring, human oversight becomes approval thresholds and escalation paths, accountability becomes identity, provenance, and audit trails, and safety requirements become enforceable boundaries and intervention mechanisms.

This is model risk management and Responsible AI extended into an operational governance architecture for autonomous decision-making systems — not a replacement for either.


## Objective Governance — What Is the Agent Optimizing?

Before determining what an AI agent is allowed to do, an institution has to answer a more fundamental question: what exactly are we asking the agent to achieve? That sounds straightforward, but it's one of the hardest problems in autonomous systems.

Traditional models are typically optimized against measurable targets — prediction accuracy, expected loss, fraud detection, response propensity, forecast error — and the model produces an output while business rules and human decision-makers determine what happens next. Agentic systems can close that loop: they observe an environment, make decisions, take actions, evaluate the results, and continue pursuing an objective across multiple steps. A poorly specified objective is therefore no longer merely a modeling problem; it can become an operational one.

### When the Metric Becomes the Objective

Consider a customer-retention agent given the objective reduce attrition among high-value customers. The objective sounds reasonable, but how should the agent achieve it? If success is measured primarily by retention rate, the agent might discover that increasingly generous pricing concessions improve the metric — attrition falls and the system looks successful, while profitability deteriorates. The same pattern shows up across the enterprise: a fraud agent told to minimize losses can produce excessive declines and customer friction; a collections agent told to maximize recoveries can become overly aggressive; a marketing agent told to maximize conversion can repeatedly target the customers most likely to respond, regardless of suitability or long-term value; a credit agent told to minimize expected losses can tighten availability past what the business intended. In each case, the agent optimizes the stated metric while producing an outcome inconsistent with the institution's broader intent.

This is a familiar problem in AI safety, known as specification gaming: a system satisfies the formal objective while violating the intent behind it. For agentic AI the consequences are larger, because the system may have the authority to act on the optimization strategy it discovers.

### Objectives Need Boundaries

Objective governance therefore requires more than specifying what to maximize or minimize — it requires defining the boundaries within which optimization may occur. A retention agent's objective might be expressed conceptually as improve customer retention, subject to constraints around profitability, pricing authority, customer eligibility, fairness, regulatory requirements, communication frequency, and escalation thresholds. That turns the objective into a constrained optimization problem rather than a single performance target, which matches how financial institutions actually think about outcomes: growth within risk appetite, reduced fraud without unacceptable customer friction, improved collections that still meet customer-treatment requirements, more automation without eliminating appropriate human oversight. Responsible autonomy requires both an objective and a set of non-negotiable boundaries around it.

### From Business Intent to Machine-Executable Policy

The harder problem is translating those boundaries into controls an autonomous system can actually follow. A policy document might say customer offers must be "appropriate," or that exceptions require "adequate review," or that AI systems should operate "fairly" — important principles, but not something an agent executing thousands of decisions can act on directly. Where possible, governance requirements need to become machine-enforceable: maximum pricing concessions, prohibited customer attributes, transaction limits, eligibility rules, minimum confidence thresholds, restricted actions, mandatory escalation conditions, and approval requirements above defined risk thresholds.

This creates a real distinction between policy intent and policy enforcement. Responsible AI establishes the principles; operational governance translates those principles into controls that actually shape what the agent does. Not every principle reduces to a deterministic rule — fairness, suitability, customer harm, and contextual judgment often still require a person. The goal isn't to encode every governance decision into software, but to make critical boundaries technically enforceable wherever practical, and to explicitly escalate what requires judgment.

### Objectives Can Conflict

Agents often operate under several legitimate objectives at once. A lending agent might need to balance credit risk, customer experience, processing speed, profitability, fair lending, and operational efficiency — and these can pull against each other: reducing processing time can raise operational risk, tightening underwriting can reduce losses while lowering approval rates, more personalization can improve conversion while raising fairness or privacy concerns.

Governance therefore needs to establish not just the objectives but their priority. Some objectives should be optimized; others should function as constraints; a few should be hard boundaries the agent cannot override regardless of expected business value — conceptually, a hierarchy running from business objective to risk constraints to regulatory boundaries to human escalation. An agent may optimize within that space, but it shouldn't be free to redefine the space itself.

### Objectives Must Remain Governed After Deployment

Objective governance doesn't end at design time. An agent may discover strategies developers didn't anticipate as data, customer behavior, economic conditions, available tools, or feedback mechanisms change — a system built to behave conservatively can gradually find more aggressive strategies that remain technically consistent with its reward structure. That's the direct link to behavioral governance: institutions need to monitor not only whether an agent is meeting its target, but how. Which actions are increasingly associated with successful outcomes? Are exceptions becoming more frequent, or is the agent approaching control limits more often? Are certain customer groups experiencing materially different outcomes? Has the agent found a strategy that technically satisfies its objective while undermining the policy's intent? Performance metrics alone can't answer these questions.

Agentic AI sharpens a governance principle that predates it: a system shouldn't be judged only by whether it achieves its objective, but by whether it does so in a way the institution actually intended. For financial institutions, that means translating business intent into objectives, constraints, escalation rules, and measurable boundaries before meaningful autonomy is granted — the first governance boundary sits before permissions, monitoring, or runtime controls, because it sits in the objective itself.

Once an institution has established what the agent should pursue and the boundaries within which it may pursue it, the next question is equally consequential: how much authority should the agent actually receive to act on those decisions?
